{% extends 'base.html.twig' %}

{% block title %}Hello Etablissement !{% endblock %}

{% block body %}

<div class="flex">
{% for etablissement in etablissements %}
   <div class="card"> 
   {{etablissement.nom}}
    <a href="{{path('etablissement_details',{id: etablissement.id})}}" >
   </div>
   {% endfor  %}
</div>
{% endblock %}
